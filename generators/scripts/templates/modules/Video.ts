import { Modu, ModuOptions } from '@malven/modu';

/**
 * Handles behavior of ambient videos.
 */

export default class extends Modu {
  constructor(m: ModuOptions) {
    super(m);
  }

  init = () => {
    // DEV: Any future logic goes here
  };
}
